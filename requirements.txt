# LiveKit related
livekit>=0.18.0                     # Core SDK
livekit-Protocol                    # Protocol
livekit-agents>=0.11.0              # Agents framework
livekit-plugins-openai>=0.10.5      # OpenAI plugin
livekit-api~=1.0                    # For room administration (token service)

# Other dependencies
python-dotenv~=1.0
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.3
pydantic-settings>=2.0.0
requests~=2.0

# Development environment
ipython>=8.0.0
black>=24.0.0
isort>=5.12.0
ruff>=0.1.0 # Linter and formatter

livekit 
livekit-protocol
livekit-agents
livekit-plugins-openai